name: check functionality

on:
  pull_request:
    branches:
      - master


jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: test
        run: echo abc