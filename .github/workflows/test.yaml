name: check functionality

on:
  pull_request_target:
    branches:
      - develop


jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: test
        run: echo def